import React from "react";
import Form from "../Form";

import "./Toolbar.css";

const Toolbar = () => (
  <div>
    <div className="wraper-toolBar">
      <div>Toolbar</div>
    </div>
  </div>
);

export default Toolbar;
